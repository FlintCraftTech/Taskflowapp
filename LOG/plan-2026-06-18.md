# [HASH] — /plan: queued a test batch for the batch 0001 TaskDao tests (deferred-test roll, now runnable on the connected Pixel 6), parked the day-boundary re-bucketing capture behind 0012, and removed the stale "Run /plan before building" note

Three queue changes, all housekeeping the changed project state made due. The batch 0001 data-foundation tests had been deferred twice over — first the app didn't build, then no device was connected. Both blockers have cleared (build compiles as of 2026-06-16, Pixel 6 connected 2026-06-17), so the ten tests are now runnable and were rolled out of Deferred tests into a new test batch at the top of Build. Only the TaskDao tests were rolled; batch 0002's data-render checks were left in Deferred tests because they still need either the 0005 add-task path or a manual DB seed, and hand-seeding wasn't worth folding into this batch. The day-boundary re-bucketing capture (tasks don't move from Tomorrow to Today if the app sits open across the 4 AM rollover) was parked behind 0012, where the fix belongs — it was already filed with that blocker, so parking only moved it into the Parked list. The "Run /plan before building" note under the Batches heading was removed: it warned that batches were blocked on open design questions in Captures, but those questions are resolved (nav-spine and spec-trim shipped) and the remaining captures are all parked behind real build dependencies, so the note now tells the user to stop when nothing blocks them.

**Queue changes:**
- Added test batch **[taskdao-instrumentation-tests]** at the top of Build; removed the rolled batch-0001 TaskDao line from Deferred tests.
- Parked the "Schedule bucketing doesn't re-evaluate across the day boundary" capture under Captures → Parked, blocked by 0012.
- Removed the stale "Run /plan before building" note under the Batches heading.

**Captures routed:** parked 1 (schedule-bucketing day-boundary); plus one deferred-test roll into the new batch.
