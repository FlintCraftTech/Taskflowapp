# b6d732f — /plan: parked [project-lifecycle-later] behind [later-by-project-screen]; split the two hybrid polish batches into combined spec-edit [polish-spec-edits] + feature-only [tomorrow-no-date-label]/[disable-drawer-swipe-open]; resolved the queue-maintenance cleanup

This session processed the two captures left after the Later-by-Project SPEC rewrite shipped, and fixed a structural issue the queue lint kept flagging.

[project-lifecycle-later] — the build work for creating, reordering, and deleting Projects in the Later-by-Project world — was parked. It attaches to surfaces (the Later cards and the editor's Project picker) that don't exist until [later-by-project-screen] ships, so it stays blocked on that. The free/paid split it proposes is real but is cleaner to pin against the actual shipped screen than to guess now, so the split is deferred to the /plan that runs when [later-by-project-screen] lands.

The queue-maintenance capture was resolved in-session rather than routed, since line-ref cleanup is /plan's to do now. Both [tomorrow-no-date-label] and [disable-drawer-swipe-open] had their bare SPEC line numbers dropped — each already names its §section and quotes the sentence it changes, so the number only drifts again the moment those spec-edits run. The parked "Empty state copy and visuals" item was rewritten for the Later-card world: its empty states are now a zero-task Schedule slot, an empty Later card, and Later before any user Project; its blocker was re-pointed off the deleted 0004 Project view onto [later-by-project-screen].

The lint flagged that both polish batches folded a one-sentence SPEC change into a feature build — which would have edited SPEC.md inside a /next feature build, exactly what the method forbids. They were split: a new combined spec-edit batch [polish-spec-edits] holds both SPEC sentence changes, and the two feature batches drop their Spec-edit subheadings and gain Depends on: [polish-spec-edits]. Combined into one spec batch rather than one per feature, to keep the extra ceremony to a single session. Leaving them hybrid was weighed and rejected — the lint is only advisory and the scope-lock allows SPEC when it's listed, so nothing else would have caught the inline edit.

**Queue changes:**
- Added [polish-spec-edits] (spec-edit batch, combined SPEC §Schedule view + §Side menu) after [spine-header-polish]; it Blocks [tomorrow-no-date-label] + [disable-drawer-swipe-open].
- [tomorrow-no-date-label] + [disable-drawer-swipe-open]: removed Spec-edit subheadings, added Depends on: [polish-spec-edits], dropped stale SPEC line numbers.
- Rewrote the parked "Empty state copy and visuals" item for the Later-card world; re-pointed its blocker to [later-by-project-screen].

**Captures routed:**
- [project-lifecycle-later] — parked (Blocked by: [later-by-project-screen]).
- [queue-maint-later-by-project] — resolved in-session and dropped (cleanup done above).
