# [HASH] — /plan: rolled the now-runnable deferred device tests (batches 0002 Schedule, 0004 Project, 0005 add/edit/complete) into one Test batch [device-verify-core-screens] at the top of Batches, ahead of 0006; emptied the Deferred tests section; left three captures unprocessed for a later /plan.

0005 landed the create path, which was the precondition the batch-0002 and batch-0004 deferred render checks were waiting on, so all three on-device checks became runnable at once. Rather than leave them scattered in the Deferred tests section, this session rolled them into a single test batch [device-verify-core-screens] and placed it at the top of Batches — ahead of 0006 (date picker) — so the whole add → render → complete loop gets verified on real hardware before later batches build on an unverified foundation. Each check is an on-device manual verification (Claude-runnable if a device is reachable, else user-run); the three deferred lines moved verbatim into the batch's Test entries and the Deferred tests section now reads empty. The session stopped after the roll, leaving three captures unprocessed for a later /plan: the spine-header polish pass, the unowned within-list task reorder-by-drag, and the new §Add "from any screen" spec-drift filed when 0005 shipped without a FAB on the Projects-overview page.

**Queue changes:**
- Added Test batch **[device-verify-core-screens]** at the top of Batches (above `### Build`), rolling the deferred 0002 / 0004 / 0005 device checks into its Test entries — runs before 0006.
- Deferred tests section emptied (the three lines moved into the new batch).

**Captures routed:** none this session — the test batch came from the deferred-test roll, not Captures. Three captures remain unprocessed: spine-header-polish, task-reorder-by-drag, spec-add-from-any-screen (§Add drift).
