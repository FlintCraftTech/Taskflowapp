# 65769ab — QUEUE.md — committed to the navigation-spine redesign: promoted right half to new spec-edit batch [nav-spine-spec-edit] (top of Build, rewrites SPEC nav model + absorbs held trim findings F3/F4/F18), reframed 0002/0003/0004 as spine work, re-parked left half as [nav-completed-history]

The top batch (0002, the four-screen Schedule view) was blocked by the parked nav redesign [nav-zoom-spine-and-completed-history], which explicitly said it would rewrite the very model 0002 builds. /next flagged the conflict and routed here. On closer reading the conflict was narrower than it looked: the four schedule screens and their date-derived task placement survive unchanged in the spine — they become four of its pages — so the spine is mostly additive navigation, not a teardown. The user committed to the spine rather than build 0002 the old way and rework it later.

Scope was split. The right half — the spine backbone (the four screens reframed as pages on a left-to-right spine that extends into Projects and Strategy), the side-menu mirror, and a Projects overview page — ships in v1. The left half — completed-history: a Search page, a Yesterday page, a day-detail card layer, and share-a-day — stays post-core, where its still-open questions (search reconciliation, share format) already sat. Yesterday was reassigned to the left half: since past-due tasks stay on Today (principle 4), Yesterday's content is essentially completed-yesterday, a completed-history concern.

The right half was promoted to a spec-edit batch, [nav-spine-spec-edit], placed at the top of Build ahead of 0002 (it blocks 0002/0003/0004). It rewrites principle 3, §Schedule view, §Side menu, and §Strategy doc into the spine model and adds the Projects overview spine page. The side-menu rewrite moves the Strategy-doc row from the top of the Projects section (placed 2026-06-16) to after the projects list — its spine position, and if anything more understated there; the user confirmed the move.

A verification pass corrected a mistake. The held spec-trim audit findings F3/F4/F18 were first noted as "superseded, drop as moot" in the re-parked item. Reading the audit log and git history showed they are neither moot nor in the right place: F3 (Schedule-view date-row layout) and F4 (rollover placement) are concrete trims still to do, owned by 0002/0012; F18 is a Projects-in-the-side-menu redundancy to consolidate. They live in the sections [nav-spine-spec-edit] rewrites, so they were folded into that batch as explicit spec-edit sub-points — finishing the deferred trim in its intended home — and the wrong note was stripped from the parked item.

The two uncommitted LOG files were hash backfills from the prior session (its unfilled hash replaced with 6fcd9d6); they ride along with this commit.

**Queue changes:**
- Added [nav-spine-spec-edit] (spec-edit batch) at the top of Build. Blocks 0002/0003/0004. Rewrites SPEC principle 3, §Schedule view, §Side menu, §Strategy doc into the right-half spine; adds a Projects overview spine page; absorbs held audit findings F3/F4/F18.
- Revised the 0002, 0003, 0004 queue summaries to spine framing; noted their archived specs are superseded on the nav points.
- Re-parked the left half as [nav-completed-history] (was [nav-zoom-spine-and-completed-history]); dropped the resolved v1-scope sub-question; corrected its F3/F4/F18 note.

**Captures routed:** [nav-zoom-spine-and-completed-history] split — right half promoted to [nav-spine-spec-edit], left half re-parked as [nav-completed-history].
