# [HASH] — /plan: promoted the ProjectDao instrumentation-test capture into a new Test batch [projectdao-instrumentation-tests] at the top of Batches — run the six ProjectDaoTest tests on the Pixel 6 and record TEST-LOG #012–017

Six ProjectDao instrumentation tests written in batch 0001 alongside the TaskDao tests were never run or recorded — the capture noting this was the last unprocessed item in Captures. Last session verified the TaskDao half of the data foundation (10/10 pass); this batch closes the gap on the Project-storage half. The tests are runnable now on the connected Pixel 6, so the batch went to a new Test subheading at the top of Batches — ahead of the 0003 feature work — to run next while the device is connected and the storage-layer context is fresh. It depends on nothing, so nothing argued for burying it. Confirmed the six tests exist in `ProjectDaoTest.kt` as the capture described before drafting.

**Queue changes:**
- Added a new `### Test` subheading at the top of Batches with batch `[projectdao-instrumentation-tests]` — run the six `ProjectDaoTest` instrumentation tests on the Pixel 6 and record TEST-LOG rows #012–017.

**Captures routed:** Promoted `projectdao-instrumentation-tests` (the sole unprocessed capture) to the new Test batch.
