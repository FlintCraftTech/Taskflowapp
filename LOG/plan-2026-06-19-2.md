# f90693b — /plan: promoted the two device-verify create-path bugs into one build batch [add-flow-create-path-fixes] at the top of Build (Today-add stores tomorrow's date — capture's date-logic diagnosis found wrong on a full read, scoped reproduce-then-fix; New-task title persists — root-caused to the per-slot ViewModel key), and dropped the stale device-verify reshape capture as already-actioned

This session processed the three captures left after the 2026-06-19 device verification.

The two create-path bugs found on device were promoted together into one build batch, [add-flow-create-path-fixes], placed at the top of Build ahead of [project-create-spec-edit]. Placement reasoning: adding a task to Today is the single most-used action and it is broken in shipped code, so a core-loop defect outranks the new project-creation feature work, and nothing depends on it to force it lower. One batch rather than two was chosen because both bugs live in the same add-flow files and one device test pass verifies both; keeping them separate was weighed and rejected as needless overhead for two small, co-located fixes.

A reading of the code changed the scope of the first bug. The capture diagnosed the Today-add date bug as the date logic or the slot-to-date mapping. A full read of the chain — the FAB's slot read (AppRoot.kt), the page-to-slot map (Destination.kt), the add ViewModel (EditTaskViewModel.kt), and the date math (SlotDeriver.kt) — showed every link computes a Today add as today's date correctly, and Soon/Later/Tomorrow all work on device. So the defect is not visible statically; the batch is scoped to reproduce it on a device first, then trace and fix, rather than apply the obvious one-line fix the capture implied. The second bug was root-caused during the session: the add screen keys its form-holder by slot (EditTaskScreen.kt), so a repeat add on the same slot reuses the prior form and its stale title; the fix is a fresh form per add.

The third capture — the device-verify "can't run as designed" reshape note — was dropped as already fully actioned: it was the input to the prior 2026-06-19 planning session (plan-2026-06-19.md), which split the batch, rolled the date-matrix checks to Deferred tests behind 0006, folded the Project checks into [project-create], and rescoped the FAB-reachable slice that then ran and left the queue. It was a leftover that had not been removed when processed.

**Queue changes:**
- Added build batch [add-flow-create-path-fixes] at the top of Build (above [project-create-spec-edit]): reproduce-and-fix the Today-slot add storing tomorrow's date, and reset the New-task form's title on each FAB open. No SPEC change.

**Captures routed:**
- Dropped: device-verify "[device-verify-core-screens] can't run as designed" reshape capture (already actioned by plan-2026-06-19.md).
- Promoted: Today-add stores tomorrow's date + New-task form keeps previous title → both into [add-flow-create-path-fixes].
