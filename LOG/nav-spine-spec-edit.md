# 21d89e5 — SPEC.md — rewrote navigation into a left-to-right spine (Today · Tomorrow · Soon · Later · Projects · Strategy): principle 3 reframed (four slots as the spine's left segment, reachable by swipe), §Schedule view re-described as pages on the spine and stripped of date-row + rollover-placement build mechanics (absorbs trim findings F3/F4), §Side menu + §Projects-in-the-side-menu merged into one spine-mirroring navigation list with the Strategy row moved to after the projects list (absorbs F18), §Strategy-doc row repositioned, and a Projects overview spine page added right of Later. → nav-spine-spec-edit.md

Taskflow's navigation is reframed from four discrete swipeable Schedule screens plus a side menu into a single left-to-right spine: Today, Tomorrow, Soon, Later, Projects, Strategy. The organising idea is zoom levels of time — a day is the finest grain, weeks are coarser, and past about a week time stops being the useful frame, so the spine glides from arranging time into arranging Projects and Strategy rather than forcing everything onto a clock. The four Schedule slots are unchanged in substance; they become the spine's left segment. This was committed in /plan on 2026-06-17. This session edits SPEC ahead of the builds (0002/0003/0004) that implement the spine, so they build against the revised product truth.

Five SPEC sections changed. Principle 3 now frames the four slots as the spine's left segment reachable by swipe, keeping "Projects are not slices of the day" and the bounded-glanceable rationale. §Schedule view describes the slots as the four leftmost pages on the spine, swiped between adjacent pages, Today still the default. §Side menu becomes one spine-mirroring navigation list, with the Strategy row moved from the top of the Projects section to after the projects list. §Strategy doc's row sentence follows that move, keeping the reachable-but-never-foregrounded intent. A new §Projects overview section describes the Projects spine page reached by swiping right past Later, with Strategy the page to its right.

The session also completed three SPEC-trim audit findings deliberately held for this rewrite, because they live in the sections being rewritten. F3 (the exhaustive date-row layout — right-edge placement, multi-line wrap, right-aligned date) and F4 (the rollover placement mechanic — appended to the bottom in existing relative order) were dropped from §Schedule view as build detail owned by 0002/0012, keeping the concepts. F18 (the doubly-described Projects-in-the-side-menu) was resolved by folding the standalone section into §Side menu. Doing the trim here finishes it in its intended home rather than redoing it later.

Approach note: the new §Projects overview section took the document slot vacated by the removed standalone §Projects-in-the-side-menu section, so the F18 consolidation and the new spine-page addition landed as a single replacement rather than a delete plus a separate insert.

**Files touched:**
- SPEC.md: principle 3, §Schedule view, §Side menu, §Strategy doc row sentence rewritten; standalone §Projects in the side menu replaced by new §Projects overview.

**Routed to Captures:** none
