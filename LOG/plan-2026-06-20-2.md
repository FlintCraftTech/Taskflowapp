# d52eb42 — /plan: parked [later-by-project] (merge the Later slot + Projects overview into one Project-grouped "Later" screen) trigger-less as a post-core right-half-spine design thread, after deciding it does NOT block the top batch [project-create] — that batch's creation plumbing is merge-independent, only the add-button location would move; folded the decision plus two design findings (the Project view survives the merge; the "removes redundancy" framing corrected) into the parked capture.

The session had one capture to process: [later-by-project], a proposal to merge the Later schedule slot and the Projects overview into a single Project-grouped "Later" screen, following the "zoom levels of time" idea already half-stated in UX principle 3. It was filed flagging that it should be resolved before [project-create] — the top build batch — because [project-create] puts Project creation on the Projects-overview page, and the merge might move that surface.

The decision was to decouple, not resolve. [project-create] proceeds as specced now; the merge is parked as its own thread. What tipped it: the hard part of [project-create] — inserting a Project at the right sort order, creating its Strategy entry, wiring the create action — is needed no matter where the merge lands. Only the add button's screen location depends on the merge, and re-homing a name-entry button later is small, so the "resolve this first" instruction was over-cautious. The alternative — hold [project-create] and settle all five of the merge's open questions now — lost because it would block core work on a five-question restructure best designed against the real screens, for no gain.

Two design findings surfaced and were folded into the parked capture so the thread inherits them. First, the per-Project view survives the merge: a Project's undated, no-slot "dreaming" tasks never appear on Later, so a Later section can't replace the Project screen — this answers the capture's open question about whether the Project view still exists. Second, the proposal's "removes a real redundancy" framing was corrected — Later-by-date and the bare Projects list are complementary today, not the same thing shown twice; the real case for the merge is the zoom coherence, not removing a duplication.

The capture was parked trigger-less, mirroring [nav-completed-history] (the parked left-end-of-spine thread); it reopens by conscious revisit when post-core navigation design is taken up.

**Queue changes:**
- [later-by-project] moved from raw Captures to Captures → Parked, rewritten to fold in the sequencing decision and the two design findings. No batches added, reordered, or removed.

**Captures routed:** [later-by-project] — parked (trigger-less).
