# [HASH] — /plan: promoted [project-create-picker-ui] restoring Project creation + retired [project-lifecycle-later] into three dependency-parked pieces; merged within-card Later reorder into [task-reorder-within-list]; split the Later ~3-task peek into [later-peek-spec-edit] + [later-card-peek]

Last session shipped the Project-grouped Later ([later-by-project-screen], 5d549be), which unblocked a cluster of follow-up work; this session routed it.

The key find: Project creation is currently impossible — the Later rebuild deleted the old Projects-overview "+ New Project" button, and the editor's Project picker (`EditTaskScreen.ProjectField`) has no "New Project" entry, so `AppViewModel.createProject` is wired to nothing. The parked [project-lifecycle-later] bundled this create path with reorder and delete under a "free buildable now / paid later" split that was wrong on two counts: free reorder needs the Strategy-doc editor (unbuilt 0015), and free delete needs a drag/bin gesture that exists nowhere yet. So the bundle was split by real dependency — the create path promoted to [project-create-picker-ui] (the only unblocked piece, and it restores a broken capability), the rest re-parked: [project-delete-later] (blocked on the general bin gesture, which Alex noted will likely arrive via task-delete first — build order doesn't matter, only that it exists), [project-reorder-strategy] (blocked on 0015), [project-lifecycle-paid] (blocked on 0020/0021 plus a SYSTEM-PROMPT.md edit).

Within-card Later reorder — split out of [later-by-project-screen] mid-build — merged into [task-reorder-within-list] rather than kept separate: the flat slots and nested cards share one drag primitive, and writing it for the flat case first risks baking in assumptions the nested case breaks, so they build together. That batch's stale rescope note (which wrongly claimed [later-by-project-screen] would build Later reorder) was corrected. The Later ~3-task peek reverses collapsed-by-default, which changes SPEC, so it split into a spec-edit ([later-peek-spec-edit]) plus feature ([later-card-peek]) pair, kept out of [polish-spec-edits] so the reversal carries its own LOG trail.

[execute-by-task-area] (temporary single-area focus across the spine) was re-cast from a fired Blocked-by to trigger-less Parked: — a transient focus-mode mechanism was floated and rejected on UI grounds, leaving the need recorded without a solution. The Project-deletion data check re-parked as [verify-project-delete-data], repointed to ride [project-delete-later]'s test pass.

**Queue changes:**
- Promoted [project-create-picker-ui] to the top of Build (restores Project creation); retired [project-lifecycle-later], re-parked as [project-delete-later], [project-reorder-strategy], [project-lifecycle-paid].
- Merged the within-card Later reorder capture into [task-reorder-within-list] (now Schedule slots + Later cards); fixed its stale rescope note.
- Added [later-peek-spec-edit] (after [polish-spec-edits]) + [later-card-peek] (before [task-reorder-within-list], Depends on the spec-edit).
- Re-cast [execute-by-task-area] to trigger-less Parked:; re-parked the Project-deletion data check as [verify-project-delete-data] (Blocked by [project-delete-later]).
- Rolled 2 deferred-test lines into [project-create-picker-ui]; trimmed the multi-Project line to its far-future-dated half (behind 0006).

**Captures routed:** Promoted — within-card Later reorder (→ merged into [task-reorder-within-list]), Later ~3-task peek (→ [later-peek-spec-edit] + [later-card-peek]). Parked — Project-deletion data check (→ [verify-project-delete-data]). Unpark candidates processed — [project-lifecycle-later] (→ split), [execute-by-task-area] (→ trigger-less park).
