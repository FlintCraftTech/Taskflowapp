# [HASH] — /plan: queued spec-edit-trim from the 20-finding SPEC audit (F3/F4/F18 held); parked nav zoom-spine + completed-history redesign; tray-fate = retain-not-delete

This session processed the two waiting captures, both gated by the plan marker forcing the SPEC-trim before feature builds.

The completed-task tray-fate question — what happens to a completed task when the tray clears — resolved as: tasks are **retained, not deleted**; they persist in the database. That alone is what batch 0005 needed, so it's unblocked. The question then opened a much larger idea: a "zoom levels of time" navigation redesign (a left-to-right spine Search · Yesterday · Today · Tomorrow · Soon · Later · Projects · Strategy, mirrored top-to-bottom in the menu), plus a completed-history surface, a day-card layer, and a share-a-day feature. Rather than let that swallow a session whose job was the trim, we took the lean route: lock only the data answer and park the whole redesign as its own design thread [nav-zoom-spine-and-completed-history] with its open sub-questions intact. The two rejected alternatives — design the nav model now and defer the trim, or design everything and merge it with the trim — lost because lean unblocks the one actually-blocked batch and avoids pinning share-format and swipe detail with no real screen to test against. An early objection — that putting Projects on the spine contradicts SPEC principle 3 ("Projects are not slices of the day") — was withdrawn: the zoom concept treats the spine as a *zoom continuum*, not a claim that Projects are a time slice, so principle 3 needs only a wording sync, not a reversal.

The SPEC-trim audit's 20 findings were decided as recommended: trim exhaustive UI mechanics to concept-level, with the archived batch specs (which already hold the detail) as the home for relocated detail, backfilling gaps. F2 keeps the slot thresholds as product truth; F20 takes no edit (coordinate with Help later); F3, F4, F18 are held because they sit in the Schedule-view and side-menu sections the parked redesign will rewrite. The resulting [spec-edit-trim] batch went to the top of Build ahead of 0002, the plan marker was removed, and the tray-retention clarification rides into that batch.

**Queue changes:**
- Added batch [spec-edit-trim] at the top of Build (ahead of 0002): SPEC trim per findings F1, F5–F17, F19; F20 no-edit; F3/F4/F18 held.
- Removed the "Plan session here: process the SPEC-trim audit findings…" marker (reason addressed).
- Parked new capture [nav-zoom-spine-and-completed-history] (navigation redesign + completed-history design thread).
- Corrected that capture: F5 removed from the superseded list (it's Project-view internal layout, not nav-affected).

**Captures routed:**
- completed-task-post-tray-fate → resolved (retain-not-delete) + overflow parked as [nav-zoom-spine-and-completed-history].
- spec-trim-audit-findings → promoted to [spec-edit-trim].

No red flags this session.
